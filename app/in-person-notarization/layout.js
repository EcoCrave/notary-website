export const metadata = {
  title: "In Person Notarization",
  description: "This is in person notarization page ",
};

export default function HowItWorksLayout({ children }) {
  return (
    <html lang="en">
      <body className={``}>{children}</body>
    </html>
  );
}
